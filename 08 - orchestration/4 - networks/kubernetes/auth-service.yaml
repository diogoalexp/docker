apiVersion: v1
kind: Service
metadata:
  name: auth-service
spec:
  selector:
    app: auth
  type: ClusterIP #dont need to expose to the public outside of the cluster
  ports:
    - protocol: TCP
      port: 80 #app port
      targetPort: 80